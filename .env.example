# PostgreSQL (compose / Railway-style split vars supported by backend)
POSTGRES_DB=supplyforge
POSTGRES_USER=supplyforge
POSTGRES_PASSWORD=change-me

# Backend JDBC (Spring converts DATABASE_URL on Railway if you add parsing later)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/supplyforge
SPRING_DATASOURCE_USERNAME=supplyforge
SPRING_DATASOURCE_PASSWORD=change-me

# Frontend (browser calls API)
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WORKSPACE_ID=1
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Gemini (map cột — chỉ gửi header + mẫu nhỏ)
GEMINI_API_KEY=AIzaSyBp0nExTxg74eo3r9J_d3f6WWYWqcDghiA
GEMINI_MODEL=gemini-1.5-flash
GEMINI_BASE_URL=https://generativelanguage.googleapis.com
